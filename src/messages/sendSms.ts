import { MobilePhone } from '../contact/Contact';
import { Message } from './Message';

export async function sendSms(contact: MobilePhone, message: Message): Promise<string> {
  // magic
  return 'xxx.yyy.zzz';
}
