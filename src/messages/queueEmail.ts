import { Email } from '../contact/Contact';
import { Message } from './Message';

export async function queueEmail(contact: Email, message: Message): Promise<string> {
  // magic
  return 'xxx.yyy.zzz';
}
