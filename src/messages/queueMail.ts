import { Address } from '../contact/Contact';
import { Message } from './Message';

export async function queueMail(contact: Address, message: Message): Promise<string> {
  // magic
  return 'xxx.yyy.zzz';
}
