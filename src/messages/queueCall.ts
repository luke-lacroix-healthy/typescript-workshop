import { Landline } from '../contact/Contact';
import { Message } from './Message';

export async function queueCall(contact: Landline, message: Message): Promise<string> {
  // magic
  return 'xxx.yyy.zzz';
}
